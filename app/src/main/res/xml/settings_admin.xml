<?xml version="1.0" encoding="utf-8"?>

<PreferenceScreen
    xmlns:android="http://schemas.android.com/apk/res/android">

    <Preference
        android:defaultValue="true"
        android:key="admins"
        android:summary="Selecciona cuales usuarios tienen permisos de administrador"
        android:title="Administradores"/>

    <SwitchPreference
        android:defaultValue="true"
        android:key="info"
        android:summary="Muestra la sección de información, donde se encuentra la descripción, ubicación y hora del evento"
        android:title="Información" />

    <SwitchPreference
        android:defaultValue="true"
        android:key="news"
        android:summary="Muestra la seccion de noticias. Al habilitarla los usuarios también podrán recibir notificaciones de las mismas"
        android:title="Noticias" />

    <SwitchPreference
        android:defaultValue="true"
        android:key="chat"
        android:summary="Muestra la sección de chat, donde los usuarios pueden interacturar, compartir enlaces e imágenes"
        android:title="Chat" />

    <SwitchPreference
        android:defaultValue="true"
        android:key="people"
        android:summary="Muestra la sección de expositores, así como, la lista expecífica de estos para cada evento del cronograma"
        android:title="Expositores" />

    <SwitchPreference
        android:defaultValue="true"
        android:key="comments"
        android:summary="Muestra la sección de expositores, así como, la lista expecífica de estos para cada evento del cronograma"
        android:title="@string/text_comments" />

</PreferenceScreen>
